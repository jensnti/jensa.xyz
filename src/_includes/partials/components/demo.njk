{% if not hideDescription %}
{{ description | safe }}
{% endif %}

{%- if not hideDemo -%}
<style>
{{- css | safe }}
</style>
{%- endif -%}

<details {{ "open" if showDetails }}>
  <summary>CSS for "{{ title }}"</summary>

```css
{{- css | safe }}
```
</details>

{%- if not hideDemo -%}
<div class="demo">
{{- html | safe }}
</div>
{%- endif -%}